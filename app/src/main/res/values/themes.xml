<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws all chrome itself; the platform theme must not add an ActionBar. -->
    <style name="Theme.ShoppiList" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
