*.iml
.gradle/
.kotlin/
/local.properties
local.properties
.DS_Store
.externalNativeBuild
.cxx

# Build output (root + every module)
build/
**/build/
/captures

# IDE — user-specific, not shared
.idea/

# Claude Code local session/config — not shared
.claude/

# Release signing — never commit real keystore material
keystore/*.jks
keystore/*.keystore
keystore/keystore.properties

# Firebase Admin SDK credentials — never commit
backend/service-account.json
backend/node_modules/
backend/package-lock.json
backend/package.json
